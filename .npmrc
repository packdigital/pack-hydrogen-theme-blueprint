@shopify:registry=https://registry.npmjs.com
progress=false
# React 19 runtime is ahead of the @pack/* packages' peerDependencies
# (which still declare react ^18). Allow installs until Pack widens its
# peer ranges to include React 19.
legacy-peer-deps=true
